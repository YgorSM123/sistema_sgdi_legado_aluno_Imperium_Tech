tags:
  - Web
summary: Dashboard gerencial (HTML)
produces:
  - text/html
parameters:
  - name: periodo
    in: query
    type: string
    enum: ["7d", "1m", "3m", "6m"]
  - name: responsavel
    in: query
    type: integer
  - name: prioridade
    in: query
    type: string
  - name: status
    in: query
    type: string
responses:
  200:
    description: Pagina HTML do dashboard
